#can just import this variable into other files instead of writing them out every time the code connects to postgreSQL
PARAMETERS = {'user':'postgres', 'password': '!clun25K', 'port': '5432', 'host': 'localhost'}
